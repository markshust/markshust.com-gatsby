import styled from 'styled-components'
import { rhythm } from '../utils/typography'

export default styled.div`
  background: #0967d2;
  padding: ${rhythm(1)};
  margin: 0 -${rhythm(0.75)} ${rhythm(1)};
  text-align: center;
  color: #fff;
  font-family: Ubuntu, sans-serif;
`
